# Port the BFF server listens on
PORT=9009

# Base URL of the backend service this BFF proxies to
BE_URL=https://backend.internal.example.com

# Rule path appended to BE_URL when constructing backend URLs
BE_RULE_PATH=regel/bekraftabeslut
